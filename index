<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>교육훈련 점수 종합</title>
    <style>
        :root { 
            --primary: #2c3e50; 
            --accent: #27ae60; 
            --bg-overlay: rgba(244, 247, 246, 0.85); 
            --container-bg: rgba(255, 255, 255, 0.92);
        }

        body { 
            font-family: -apple-system, 'Pretendard', sans-serif; 
            margin: 0; 
            padding: 20px 10px;
            color: var(--primary);
            /* 파일명이 background.jpg 인지 꼭 확인하세요! */
            background-image: linear-gradient(var(--bg-overlay), var(--bg-overlay)), url('background.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }

        .container { 
            max-width: 800px; 
            margin: 0 auto; 
            background: var(--container-bg); 
            padding: 30px; 
            border-radius: 16px; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.2); 
            backdrop-filter: blur(5px);
        }
        
        h1 { text-align: center; margin-bottom: 25px; font-size: 1.8rem; color: var(--primary); }

        .result-box { 
            margin-bottom: 30px; 
            padding: 25px; 
            background: #34495e; 
            color: white; 
            border-radius: 12px; 
            text-align: center; 
        }
        .total-score { font-size: 4rem; font-weight: bold; color: #2ecc71; margin: 10px 0; }

        .table-wrapper { width: 100%; overflow-x: auto; margin-bottom: 25px; border-radius: 8px; }
        table { width: 100%; border-collapse: collapse; min-width: 580px; background: white; }
        th { background: #f1f3f5; color: var(--primary); padding: 15px; font-size: 0.95rem; border-bottom: 2px solid #dee2e6; }
        td { padding: 15px 10px; border-bottom: 1px solid #eee; text-align: center; }
        
        input[type="number"], select { 
            width: 100%; 
            max-width: 120px; 
            padding: 10px; 
            border: 2px solid #ddd; 
            border-radius: 8px; 
            text-align: center; 
            font-size: 1rem;
        }
        /* 입력 비활성화 스타일 */
        input:disabled { background-color: #e9ecef; cursor: not-allowed; border-color: #dee2e6; }

        .grade-badge { padding: 6px 12px; border-radius: 6px; font-weight: bold; background: #2c3e50; color: white; }
        .point-display { font-weight: bold; color: #d35400; font-size: 1.2rem; }
        .info-text { font-size: 0.9rem; color: #666; text-align: center; margin-top: 20px; }
    </style>
</head>
<body>

<div class="container">
    <h1>교육훈련 점수 종합</h1>
    
    <div class="result-box">
        <div class="total-label">환산 점수 총합</div>
        <div id="totalDisplay" class="total-score">0.00</div>
        <div class="total-max">/ 70.00 점</div>
    </div>

    <div class="table-
